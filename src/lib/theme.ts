import { createTheme } from '@mui/system';

export const theme = createTheme({
    palette: {
        paper: {
            main: '#121330'
        },
        primary: {
            main: '#9900ff'
        }
    }
})